import React from 'react';

// Stateless functional component (we just need to display a content)
const NotFound = () => {
	return(
		<h2 className="notFound">Nothing here!</h2>
	)
}

export default NotFound;
